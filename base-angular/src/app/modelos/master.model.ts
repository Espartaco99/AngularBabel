export class Printer {
    // Como hacer la declaracion y asignacion de valores en la constructora,
    // metodo abreviado
    constructor(public id: string, public name: string) {}
}
export interface Departamentos {
    id: String;
    name: String;
}
export interface UserIf {
    nombre: string;
    apellidos: string;
    telefono: string;
}
